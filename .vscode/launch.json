{
    "configurations": [
        {
            "name": "FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
                "--reload"
            ],
            "envFile": "${workspaceFolder}/.env",
            "jinja": true
        }
    ]
}